import { useLiveData } from "../../hooks/useLiveData";
export default function PolicyInsights() { const {data}=useLiveData(); const price=data?.clearing_price; return <section className="card insights"><h3>MAPPO policy readout</h3><p>The centralized critic coordinates 21 agent policies against the 33-bus safety constraints. Live price is <b>Rs {price?.toFixed(2) ?? "--"}/kWh</b>.</p><div><span>Solar agents</span><strong>{data?.grid_import_kw > 0 ? "Prioritize local supply" : "Sell surplus"}</strong></div><div><span>College battery</span><strong>{data?.agents?.college?.soc > .6 ? "Dispatch available" : "Preserve SoC"}</strong></div><div><span>Consumers</span><strong>Bid below grid tariff (Rs 8.15)</strong></div></section>; }
