import { useLiveData } from "../../hooks/useLiveData";
export default function GridTopology() { const {data}=useLiveData(); const nodes=Array.from({length:33},(_,i)=>({id:i+1,x:70+(i%11)*80,y:75+Math.floor(i/11)*92})); return <section className="card topology"><h3>IEEE 33-bus campus network <span>{data?.grid_import_kw?.toFixed(1) ?? "--"} kW grid import</span></h3><svg viewBox="0 0 940 310" role="img" aria-label="33 bus smart-grid topology">{nodes.slice(1).map((n,i)=><line key={`line${n.id}`} x1={nodes[i].x} y1={nodes[i].y} x2={n.x} y2={n.y} />)}{nodes.map(n=><g key={n.id}><circle cx={n.x} cy={n.y} r={n.id===7?12:7} className={n.id===7?"college-bus":n.id>7&&n.id<22?"solar-bus":"bus"}/><text x={n.x} y={n.y+25}>{n.id}</text></g>)}</svg><p className="empty">Bus 7 hosts the college battery. Buses 8–21 host solar prosumers; buses 22–26 host consumer agents.</p></section>; }
