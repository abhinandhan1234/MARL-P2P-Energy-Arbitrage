import { useLiveData } from "../../hooks/useLiveData";
export default function TradeHistory() { const {data,isLoading}=useLiveData(); return <section className="card"><h3>Completed trades</h3>{isLoading?<div className="spinner"/>:<div className="table-wrap"><table><thead><tr><th>Trade</th><th>Route</th><th>Energy</th><th>Price</th><th>Status</th></tr></thead><tbody>{(data?.recent_trades||[]).map(t=><tr key={t.trade_id}><td>{t.trade_id.slice(-6)}</td><td>{t.seller} → {t.buyer}</td><td>{t.volume_kwh} kWh</td><td>Rs {t.price_rs_per_kwh}</td><td><span className="tag">{t.status}</span></td></tr>)}{!data?.recent_trades?.length&&<tr><td colSpan="5" className="empty">No completed orders.</td></tr>}</tbody></table></div>}</section>; }
