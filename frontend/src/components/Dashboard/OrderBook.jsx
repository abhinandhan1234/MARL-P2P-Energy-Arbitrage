import { useLiveData } from "../../hooks/useLiveData";
export default function OrderBook() { const { data } = useLiveData(); const trades = data?.recent_trades || []; return <section className="card"><h3>Recent orders</h3><div className="order-head"><span>Seller</span><span>Volume</span><span>Price</span></div>{trades.length ? trades.slice(0, 6).map((t) => <div className="order" key={t.trade_id}><span>{t.seller}</span><span>{t.volume_kwh} kWh</span><strong>Rs {t.price_rs_per_kwh}</strong></div>) : <p className="empty">No manual trades yet. Submit a live order in Marketplace.</p>}</section>; }
